# Required
ADMIN_PASSWORD=changeme

# Optional - defaults shown
SITE_NAME=Status Page
SITE_DESCRIPTION=Current status of our services
DATABASE_PATH=/app/data/status-page.db

# SMTP (optional - needed for subscriber notifications)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=status@example.com

# Base URL for links in emails
BASE_URL=http://localhost:3000
